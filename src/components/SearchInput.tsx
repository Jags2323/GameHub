import { Input } from "@chakra-ui/react";

const SearchInput = () => {
  return (
    <div>
      <Input borderRadius={20} placeholder="seacrh games.." variant="filled" />
    </div>
  );
};

export default SearchInput;
